<form
    class="max-w-screen-sm"
    data-netlify="true"
    method="POST"
    name="contact"
    netlify-honeypot="bot-field">
    <p class="hidden">
        <label>
            Don’t fill this out if you're human: <input name="bot-field" />
        </label>
    </p>
    {% render 'input', label: "Name:", name:"name", required: true %}
    {% render 'input', label: "Email:", name:"email", type: "email", required: true %}
    {% render 'input', label: "Message:", name:"message", type: "textarea", required: true %}
    {% render 'button', label: "Send", type: "submit" %}


</form>
